version: '3.8'

services:
  web:
    build: .
    container_name: pg-web1
    ports:
      - "80:80"
    depends_on:
      - db

  db:
    image: mysql:5.7
    container_name: pg-db1
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: campus_stay
    ports:
      - "3306:3306"
